# Benchmark run output (regenerated by scripts/run-benchmark.sh; results.md
# at the repo root is the committed artifact, this raw data is not)
/benchmark-artifacts/
# k6 writes this in the project dir via handleSummary() before
# run-benchmark.sh moves it into benchmark-artifacts/ — ignore any that's
# left behind by an interrupted run
**/summary.json

# Rust
axum-item-crud/target/

# Node/Bun
bun-item-crud/node_modules/
bun-item-crud/bun.lockb

# OS cruft
.DS_Store
axum-item-crud/Cargo.lock
